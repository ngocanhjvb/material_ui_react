import {makeStyles} from "@material-ui/core/styles";


export const useStyles = makeStyles((theme) => ({
    timeFilterBox: {
        display: 'flex',
        alignItems: 'center',
    },
    timeFilterButton: theme.buttonStyle
}));
